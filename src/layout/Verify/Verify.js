import React, { useContext, useEffect, useState } from "react";
import { AuthContext } from "../../firebase/AuthProvider";
import { GoVerified } from "react-icons/go";

const Verify = ({ ver, setGetUser }) => {
  return (
    <div>
      <div></div>
    </div>
  );
};

export default Verify;
